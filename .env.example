# All values are server-only. Never add a VITE_ prefix to these names.
# Populate .env.local for a new checkout; keep the existing file if configured.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/gym_log?sslmode=verify-full"
NEON_AUTH_BASE_URL="https://ep-crimson-moon-b3xsjaou.neonauth.c-4.ap-southeast-1.aws.neon.tech/gym_log/auth"
# Generate a random secret of at least 32 characters. Required; no default.
NEON_AUTH_COOKIE_SECRET=""
APP_ORIGIN="http://localhost:5173"
PORT=5173
